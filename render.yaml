services:
  # Django Backend
  - type: web
    name: quiz-backend
    runtime: python
    buildCommand: ./build.sh
    startCommand: gunicorn quiz_portal.wsgi:application
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: quiz-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: PYTHON_VERSION
        value: "3.12.2"

  # React Frontend (Static Site)
  - type: web
    name: quiz-frontend
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: we/vite-project/dist
    rootDir: we/vite-project
    envVars:
      - key: VITE_API_URL
        fromService:
          name: quiz-backend
          type: web
          property: url
      - key: VITE_SUPABASE_URL
        value: https://schmjafbheslmcrzsrmi.supabase.co
      - key: VITE_SUPABASE_ANON_KEY
        value: sb_publishable_JCiHXz2_zJHrA-3LRI8psA_p7Jd3TAL

databases:
  - name: quiz-db
    databaseName: quizportal
    user: quizuser
    plan: free
